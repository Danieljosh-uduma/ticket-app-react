export type userData = {
    fullname: string,
    email: string,
    password: string
    phoneNumber: string
}

export type ticketType = {
        title: string
        status: string
        priority: string
        description: string
    }